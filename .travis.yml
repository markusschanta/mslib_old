language: python
python:
  - "2.7"
#  - "3.4" # Comment out to speed up build
  - "3.5"
  - "3.6"
install:
  - pip install -r requirements.txt
  - pip install .
# command to run tests
script:
  - pytest
